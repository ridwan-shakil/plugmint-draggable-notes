<?php
/**
 * REST API (Unused for now).
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

class Admin_Notes_REST {

	public function init() {
		// add_action( 'rest_api_init', array( $this, 'register_routes' ) );
	}

	public function register_routes() {
		// Register future REST routes.
	}
}
